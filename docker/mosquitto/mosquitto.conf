listener 1883
protocol mqtt

listener 9001
protocol websockets

# Local dev only: anonymous access with no ACLs. Per-topic ACLs and ephemeral
# per-dispatcher credentials are the scope of change 02-add-fleet-auth, not
# this scaffolding change. Do not treat this file as the production posture.
allow_anonymous true

persistence false
log_dest stdout
