{
  "name": "api-client",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/api-client/src",
  "tags": ["type:generated"],
  "targets": {
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/api-client/generate.mjs",
        "cwd": "{workspaceRoot}"
      }
    },
    "check-drift": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/api-client/generate.mjs --check",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
