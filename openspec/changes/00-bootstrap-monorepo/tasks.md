# Tasks: Bootstrap Monorepo

## 1. Workspace Nx
- [x] 1.1 Crear workspace Nx
- [x] 1.2 Generar `apps/console` (Angular 21, standalone, zoneless)
- [x] 1.3 Instalar y configurar el plugin `@nx/gradle`
- [x] 1.4 Verificar que `nx graph` muestra los módulos Gradle junto al proyecto Angular (parcial, sin cambios respecto al cierre de la sección 1: el lado Gradle está probado directamente — `./gradlew.bat nxProjectGraph` genera `build/nx/*.json` por módulo con las aristas de dependencia reales, p. ej. `api -> domain`, `api -> geo-core` — pero `npx nx show projects`/`nx graph` todavía fallan en esta máquina porque `@nx/gradle` invoca `gradlew.bat` con `child_process.execFile(..., { shell: true })` sin comillas alrededor de una ruta con espacios (`C:\Henry\Mis proyectos\Fleetpulse`); reproducido de forma aislada con Node puro, ver informe de aplicación de la sección 2. No es un problema de configuración de este repo, es una limitación de `@nx/gradle` con rutas de workspace que contienen espacios en Windows)

## 2. Build Gradle
- [x] 2.1 `backend/settings.gradle.kts` con los módulos `api`, `processor`, `domain`, `geo-core`
- [x] 2.2 Spring Boot 4.1 y Java 21 en el build raíz; `dev.nx.gradle.project-graph` aplicado a `allprojects`
- [x] 2.3 `geo-core` sin dependencia de Spring ni de JPA (verificado por una comprobación del build)
- [x] 2.4 `api` y `processor` dependen de `domain` y `geo-core`, nunca entre sí

## 3. Datos
- [x] 3.1 `docker-compose.yml` con PostGIS y Mosquitto, ambos con healthcheck
- [x] 3.2 `mosquitto.conf` con listener MQTT y listener WebSocket
- [x] 3.3 Flyway configurado en `domain`; migración `V1__init.sql` con `CREATE EXTENSION postgis` y `pg_partman`
- [x] 3.4 Perfiles de Spring: `local`, `test`, `prod`; validación de configuración que impide arrancar sin variables obligatorias

## 4. Contrato Java → TypeScript
- [x] 4.1 `springdoc-openapi` en `api`, exponiendo el documento OpenAPI (`org.springdoc:springdoc-openapi-starter-webmvc-ui:3.1.0` — la línea 2.x de springdoc no soporta Spring Boot 4/Spring Framework 7; verificado contra Maven Central que 3.1.0 hereda de `spring-boot-starter-parent:4.1.0`. Test `OpenApiDocumentPublicationTest` con `@SpringBootTest(webEnvironment = RANDOM_PORT)` real: arranca el contexto completo con `spring.autoconfigure.exclude` sobre `DataSourceAutoConfiguration`/`HibernateJpaAutoConfiguration`/`FlywayAutoConfiguration` — en Spring Boot 4.1 estas clases viven en paquetes nuevos (`org.springframework.boot.jdbc.autoconfigure.*`, `org.springframework.boot.hibernate.autoconfigure.*`, `org.springframework.boot.flyway.autoconfigure.*`) — para no depender de una base de datos real solo para publicar el documento OpenAPI. RED confirmado antes de añadir la dependencia (404 en `/v3/api-docs`), GREEN después)
- [x] 4.2 Tarea que genera `libs/api-client` con `openapi-generator` (`scripts/api-client/generate.mjs`, expuesto como `npm run generate:api-client` y como target Nx `api-client:generate` en `libs/api-client/project.json`. El script compila `api:bootJar`, arranca el jar con las mismas autoconfiguraciones excluidas que en 4.1, pide `/v3/api-docs`, y genera con `openapi-generator` (`typescript-angular`) hacia `libs/api-client/src`. Salida generada commiteada, no en `.gitignore`; marcada como generada en `.gitattributes` (`linguist-generated=true`) y excluida de ESLint/Prettier. Como no hay endpoints reales todavía, el cliente generado es un scaffold casi vacío (`export const APIS = [];`) — es lo esperado. Añadido mapeo de path `@fleetpulse/api-client` en `tsconfig.base.json` para cuando `apps/console` empiece a consumirlo. Nota Windows: `openapi-generator-cli` (el wrapper npm) corta mal argumentos con espacios — p. ej. `-o "C:\...\Mis proyectos\..."` — al reenviarlos a su proceso Java interno; mismo tipo de bug ya documentado para `@nx/gradle` en la tarea 1.4 pero en el paquete `@openapitools/openapi-generator-cli`, no en Nx. Mitigado generando siempre en un directorio temporal sin espacios y copiando el resultado con `fs.cp` de Node en vez de pasarle a la CLI una ruta de salida dentro del workspace)
- [x] 4.3 Paso de CI que regenera y falla si hay diferencias con lo commiteado (mismo script con `--check`, expuesto como `npm run generate:api-client:check` y como target Nx `api-client:check-drift`. Regenera en un directorio temporal y compara archivo por archivo contra `libs/api-client/src`; sin diferencias, exit 0; con diferencias, exit 1 y lista los archivos afectados. Verificado en local: contra una generación limpia pasa (GREEN); tras editar a mano `libs/api-client/src/variables.ts` falla señalando exactamente ese archivo (RED); revertido el cambio manual vuelve a pasar (GREEN). Esta tarea entrega el script ejecutable y verificado — el cableado real en `.github/workflows/ci.yml` (paso de CI, servicios, caché) es de la sección 6, batch 6)

## 5. Salud y observabilidad
- [x] 5.1 `/actuator/health` con indicadores de base de datos y de broker (mitad base de datos: indicador auto-configurado de `DataSource` de Spring Boot, sin código propio. Mitad broker: `MqttBrokerHealthIndicator` en `api` y en `processor`, cada uno respaldado por el bean `MqttPahoClientFactory` de `MqttClientFactoryConfig` — timeout de 5s, sin reconexión automática, conexión de corta duración abierta solo para el chequeo. Verificado con `MqttBrokerHealthIndicatorTest` en ambos módulos contra un Mosquitto real vía Testcontainers — 2 tests cada uno: UP cuando el broker es alcanzable, DOWN cuando no lo es. Verificado extremo a extremo con `ActuatorHealthEndpointTest` en `api` — `@SpringBootTest(webEnvironment=RANDOM_PORT)` real contra PostGIS+pg_partman y Mosquitto reales vía Testcontainers, confirma que `/actuator/health` reporta `db` y `mqttBroker` ambos UP)
- [x] 5.2 Exponer `commit` (SHA inyectado en el build) en el endpoint de información (plugin `com.gorylenko.gradle-git-properties` + `springBoot { buildInfo() }` en `api` y `processor`; `management.endpoints.web.exposure.include: health,info`, `management.endpoint.health.show-details: always` y `management.info.git.mode: full` en `application.yml` de `api`. `CommitHealthIndicator` en `api` reporta DOWN si `GitProperties`/el SHA no están disponibles en el build — cubierto por `CommitHealthIndicatorTest`, 2 tests unitarios: UP con detalle `sha` cuando `GitProperties` está presente, DOWN cuando falta. Verificado extremo a extremo con `ActuatorInfoEndpointTest`, que confirma que `/actuator/info` expone el SHA de 40 caracteres del commit realmente desplegado, comparado contra `git rev-parse HEAD`. Bug real encontrado y corregido durante la reverificación obligatoria de esta tanda: con `management.info.git.mode: full`, Spring anida `git.commit.id` en un objeto `{describe, abbrev, full}` en vez de exponerlo como cadena plana — eso solo ocurre en modo `simple`. RED confirmado (`JsonNodeException`: `ObjectNode` no se puede convertir a `String`) al ejecutar el test heredado sin verificar de una sesión anterior; GREEN tras corregir el test para leer `git.commit.id.full`)
- [x] 5.3 `processor` publica latido retenido periódicamente; `api` lo expone en su salud (`FleetpulseHeartbeatProperties` añadido como sibling de `FleetpulseMqttProperties` en `config` de ambos módulos, prefijo `fleetpulse.mqtt.heartbeat` — asimétrico a propósito: `processor` solo necesita `topic` (`@DefaultValue("fleetpulse/processor/heartbeat")`), `api` necesita además `stalenessThreshold` (`@DefaultValue("90s")`, margen 3x sobre el intervalo de publicación). `ProcessorHeartbeatPublisher` (`dev.fleetpulse.processor.heartbeat`, nuevo `@Component` con `@Scheduled(fixedRate = 30_000L)`, constante fija — mismo criterio que el timeout de conexión ya hardcodeado en `MqttClientFactoryConfig`) publica cada 30s un mensaje MQTT retenido (QoS 1) con `Instant.now().toString()` como payload, usando el `MqttPahoClientFactory` ya existente, con conexión de corta duración igual que los indicadores de salud. `ProcessorHeartbeatHealthIndicator` en `api` (mismo patrón de conexión corta que `MqttBrokerHealthIndicator`, deliberadamente sin un adaptador de canal de entrada de Spring Integration de larga duración — un mensaje retenido se redistribuye de inmediato al hacer SUBSCRIBE, así que no hace falta mantener una conexión abierta) se suscribe al tópico, espera hasta 3s el mensaje retenido y reporta DOWN si no llega ninguno, DOWN si el payload no parsea como `Instant`, DOWN si la antigüedad supera `stalenessThreshold`, UP en otro caso con el detalle `lastHeartbeat`. Verificado con `ProcessorHeartbeatPublisherTest` (Testcontainers Mosquitto real, 2 tests: timestamp fresco tras publicar, y una segunda publicación reemplaza el mensaje retenido con un timestamp más nuevo) y `ProcessorHeartbeatHealthIndicatorTest` (Testcontainers Mosquitto real, 3 tests: latido fresco → UP, latido con más de 10 minutos de antigüedad y umbral de 60s → DOWN, ningún latido publicado nunca → DOWN — cada test usa un tópico aleatorio propio para evitar contaminación entre tests contra el broker compartido). `ActuatorHealthEndpointTest` extendido para publicar un latido fresco antes de consultar `/actuator/health` y confirmar que el componente `processorHeartbeat` — clave derivada automáticamente por Spring del nombre del bean, sin registro manual — también aparece UP, cerrando el circuito extremo a extremo)

## 6. CI/CD
- [x] 6.1 `ci.yml` con `nx affected` cubriendo tareas Gradle y de Angular (`.github/workflows/ci.yml`, job `affected`: `actions/setup-node` + `actions/setup-java` (Temurin 21, pinned — ver "Riesgo documentado"), `nrwl/nx-set-shas` para calcular la base/head correctas en push y en PR, y `npx nx affected -t lint test build --parallel=3`. Cubre Gradle a través de la integración `@nx/gradle` ya registrada en `nx.json` (targets `test`/`classes`/`build` inferidos) y Angular a través de los targets propios de `apps/console` (`lint`/`test`/`build`). El mismo job ejecuta también `npm run generate:api-client:check` de forma incondicional — no encadenado a "affected" — cerrando la última pieza pendiente de la tarea 4.3 y la 4ª entrada de "Definición de terminado". Problema real encontrado y corregido al verificar este paso: el blob commiteado de `libs/api-client/src` ya era LF (confirmado con `git show HEAD:...`), pero el checkout local en esta máquina Windows (`core.autocrlf=true`) lo materializaba en disco como CRLF, mientras que `scripts/api-client/generate.mjs` copia la salida del generador tal cual (LF) — el `--check` fallaba en el 100% de los archivos por fin de línea. Esto NO habría afectado al pipeline de CI real (los runners Linux de GitHub Actions no reescriben LF a CRLF al hacer checkout), pero sí es un falso positivo permanente para cualquier contribuidor en Windows con la configuración por defecto de Git. Corregido de forma preventiva añadiendo `text eol=lf` a la regla ya existente de `.gitattributes` para `libs/api-client/src/**` y regenerando el cliente en el sitio (el contenido resultante coincide byte a byte con el ya commiteado, por eso no aparece como cambio en el diff de este PR); verificado en local: `npm run generate:api-client:check` da GREEN ("in sync") tras la corrección — la detección de una desincronización real (RED) ya estaba probada en la tarea 4.3 y el algoritmo de comparación no se tocó, solo la codificación del checkout local. Esta máquina Windows no puede ejecutar `nx affected`/`nx graph` directamente por el mismo bug de `@nx/gradle` con rutas con espacios ya documentado en la tarea 1.4, así que la verificación real ocurrió en el propio pipeline de este PR (run real en GitHub Actions: https://github.com/HenrySanguna/fleetpulse/actions/runs/33736468446, job `affected`, GREEN): `nx-set-shas` resolvió `NX_BASE`/`NX_HEAD` correctamente contra la base real del PR; el commit de este batch (que no toca `apps/console` ni ningún módulo Gradle específico, solo `backend/gradlew` y archivos de infraestructura en la raíz) afectó únicamente al proyecto raíz `fleetpulse-backend` — "Running target build for project fleetpulse-backend and 3 tasks it depends on" — sin mencionar `console` en ningún punto del log; y `npm run generate:api-client:check` imprimió "libs/api-client is in sync with the OpenAPI contract published by backend/api." en ese mismo runner Linux)
- [x] 6.2 Servicios de CI: PostGIS y Mosquitto con healthcheck (job `docker-and-compose-smoke` en `.github/workflows/ci.yml`: reutiliza `docker-compose.yml` tal cual — `docker compose -p fleetpulse-ci up -d --wait`, que bloquea hasta que los healthchecks ya definidos en ese archivo para `postgis` (`docker/postgis-partman/Dockerfile`) y `mosquitto` (`docker/mosquitto/mosquitto.conf`) pasen a "healthy" — los mismos servicios que ya usan los tests Gradle con Testcontainers y el `docker-compose.yml` de desarrollo local, sin duplicar definiciones. Verificado en local con Docker Desktop real, ver detalle en 6.4)
- [x] 6.3 Caché de dependencias de Gradle y de node_modules (`actions/setup-node@v7` con `cache: npm`, clave derivada de `package-lock.json`; `actions/setup-java@v6` con `cache: gradle`, que cachea `~/.gradle/caches` y los wrappers descargados a partir de los hashes de `backend/**/*.gradle.kts` y `gradle-wrapper.properties` — mecanismo estándar documentado por ambas actions oficiales, no una caché casera. No verificable de forma significativa con una sola ejecución de CI — el efecto (miss en la primera corrida, hit en las siguientes) solo se observa comparando al menos dos runs reales; queda para observación en corridas futuras de este mismo pipeline, no es una limitación de la configuración)
- [x] 6.4 Dockerfile multi-stage: build de Gradle, imagen final con ambos jars (api + processor) y usuario no root (`Dockerfile` en la raíz: etapa `builder` sobre `eclipse-temurin:21-jdk` que compila `:api:bootJar :processor:bootJar` con `--mount=type=cache,target=/root/.gradle`; etapa `runtime` sobre `eclipse-temurin:21-jre-alpine`, usuario `fleetpulse` no root vía `addgroup -S`/`adduser -S`, ambos jars copiados con nombre fijo (`api.jar`/`processor.jar`, filtrando el `-plain.jar` que también produce `bootJar`). `docker/backend/entrypoint.sh` decide qué jar arrancar según `FLEETPULSE_PROCESS` (en esta tanda tenía además un fallback a `FLY_PROCESS_GROUP` para encajar sin cambios con `fly.toml`, tarea 6.5; ese fallback se retiró en la tanda de migración a Oracle Cloud — ver nota de la tarea 6.5 — porque Fly.io dejó de ser el destino de despliegue y mantener el fallback habría sido código muerto engañoso) — materializa "una sola imagen con ambos jars, el comando de arranque decide cuál corre" (design.md, "Dos procesos, un artefacto"). `docker/backend/healthcheck.sh` distingue ambos procesos: `api` usa el propio `/actuator/health` (Spring Boot mapea DOWN a HTTP 503 por defecto, así que un `wget --spider` ya detecta un indicador caído, no solo un proceso muerto); `processor` no expone HTTP (ver design.md, "Salud"), así que su único chequeo posible desde dentro del contenedor es que el PID 1 siga vivo — su latido se observa desde fuera, en el indicador `processorHeartbeat` de `api`, tal como pretende el diseño. Bug real encontrado y corregido durante la verificación, con dos causas distintas: (1) `backend/gradlew` estaba commiteado con modo `100644` (sin bit de ejecución) en vez de `100755` — esto SÍ es un defecto del repositorio en sí, reproducible en cualquier plataforma (incluido un runner Linux de CI), corregido con `git update-index --chmod=+x backend/gradlew`; (2) el blob de `backend/gradlew` ya era LF-only en git, pero el checkout local en esta máquina Windows lo convertía a CRLF en disco (mismo mecanismo que el hallazgo de la tarea 6.1 sobre `libs/api-client`), y como la etapa `builder` invoca el script con `sh gradlew` (no `./gradlew`, precisamente para no depender del bit de ejecución), el `docker build` local fallaba con "not found"/"syntax error" bajo `/bin/sh` — este segundo problema es específico del checkout en Windows, no habría ocurrido en un runner Linux. Corregido de forma preventiva con una regla `text eol=lf` en `.gitattributes` para `backend/gradlew` y para los archivos nuevos de este batch (`docker/**/*.sh`, `Dockerfile`, `.dockerignore`, `fly.toml`, `.github/workflows/*.yml`), mismo tipo de problema de checkout en Windows ya documentado para `@nx/gradle` en 1.4 y `openapi-generator-cli` en 4.2. `.dockerignore` deja fuera `node_modules`, `apps`, `libs`, `dist`, `.nx`, `.angular`, `openspec`, `scripts` y el `build/`/`.gradle` locales, pero deliberadamente NO excluye `.git`: el plugin `gradle-git-properties` lo necesita para incrustar el SHA real en `git.properties`, que expone `/actuator/info`/`/actuator/health` (tarea 5.2) — `.git` nunca llega a la imagen final porque la etapa `builder` se descarta. Verificado end-to-end real con Docker Desktop, sin fingir nada: `docker build` completo (BUILD SUCCESSFUL, 20 tareas Gradle); imagen inspeccionada — ambos jars presentes, propietario y usuario en ejecución `fleetpulse` (uid 100, no root); `docker compose -p fleetpulse-ci up -d --wait` levanta `postgis`+`mosquitto` reales y sanos; contenedor `api` arrancado contra ellos con las mismas variables de entorno que usará el despliegue (`SPRING_DATASOURCE_URL`, `..._USERNAME`, `..._PASSWORD`, `FLEETPULSE_MQTT_BROKERURL`) — Flyway migra el esquema real (`V1__init.sql`, PostGIS + pg_partman) al arrancar, `/actuator/health` responde con `db` UP, `mqttBroker` UP, `commit.sha` = el SHA real de HEAD (`f43156ee84f592631ad536bbf6a21a46496c901f`), y `processorHeartbeat` correctamente DOWN porque `processor` todavía no corría (prueba de que el indicador está conectado a algo real, no siempre UP); arrancado el contenedor `processor`, en menos de 30s el `processorHeartbeat` de `api` pasa a UP con un `lastHeartbeat` fresco y el estado agregado pasa a UP; el propio `HEALTHCHECK` de Docker del contenedor `api` transiciona de `unhealthy` (mientras el agregado era DOWN) a `healthy` (una vez UP) sin intervención manual. El guion embebido en el job `docker-and-compose-smoke` de `ci.yml` es exactamente el mismo que se ejecutó en este verificación manual — se extrajo el YAML con `js-yaml` y se corrió tal cual contra Docker real, exit code 0)
- [x] 6.5 Despliegue de los dos procesos de backend y de la consola (backend desplegado y verificado en vivo en Oracle Cloud; consola/Cloudflare Pages sigue pendiente, sin cambios respecto a las tandas anteriores. Esta tanda corrigió el destino de hosting completo, no solo lo cableó: se sustituye Fly.io por **Oracle Cloud "Always Free"** (una sola VM Ampere A1), porque project.md línea ~50 exige tiers gratuitos permanentes y Fly.io eliminó el suyo en 2024 — ahora requiere tarjeta y factura por uso, ~$8-9/mes para la huella de este proyecto, verificado con investigación web en vivo en esta sesión —, un incumplimiento directo de esa regla que las tandas de Fly anteriores (6, 6b) no habían detectado porque no se revalidó el estado del tier tras escribir `fly.toml`/`fly-broker.toml`. Oracle Always Free Ampere A1 (2 OCPU/12GB RAM en los términos vigentes — NO la cifra de "4 OCPU/24GB" que aún circula en documentación/blogs desactualizados, Oracle la recortó silenciosamente en 2026) es gratuita mientras el uso quede dentro de cuota y es una VM real, no serverless/scale-to-zero — requisito que `processor` necesita porque corre un consumidor MQTT de larga duración más tareas `@Scheduled` en segundo plano y no puede activarse por petición. Se eliminan `fly.toml` y `fly-broker.toml` (superados) y sus reglas en `.gitattributes`. Se añade `docker-compose.prod.yml` en la raíz: 3 servicios sobre imágenes ya construidas por CI — `api` y `processor` (la misma imagen del `Dockerfile` multi-stage raíz, diferenciados por `FLEETPULSE_PROCESS`, reutilizando la lógica de `docker/backend/entrypoint.sh`; su fallback a `FLY_PROCESS_GROUP` se retiró en esta tanda — no se dejó como código muerto inofensivo, porque mantenerlo sugeriría que Fly sigue siendo un destino soportado cuando dejó de serlo, y tanto `docker-compose.prod.yml` como el smoke test de CI ya fijan `FLEETPULSE_PROCESS` explícitamente en todos los casos, así que el fallback nunca se habría ejercitado) y `mosquitto` (reutiliza `docker/mosquitto/mosquitto.prod.conf` tal cual ya existía de la tanda anterior — mismo razonamiento de privado/sin WebSocket público: ese diseño no cambia solo porque cambió el proveedor, sigue justificado por el alcance de proposal.md y el diferimiento de ACL del change 02 en project.md). Solo `api` publica puerto en la IP pública de la VM; `processor` y `mosquitto` solo son alcanzables por la red del propio Compose — la red bridge/DNS interno de Docker es el equivalente directo de la red privada 6PN que daba Fly, y de hecho ya estaba probado en este mismo repo antes de esta tanda: el job `docker-and-compose-smoke` de CI (tarea 6.2) ya resuelve `postgis`/`mosquitto` por nombre de servicio de la misma manera, así que no es una suposición nueva sino un patrón ya verificado en CI real. Despliegue cableado en CI, no ejecutable en esta sesión (no hay cuenta/CLI/credenciales de Oracle disponibles, misma limitación que bloqueó cada tanda de Fly anterior): `deploy-oracle`, nuevo job en `ci.yml` que sustituye a `deploy-backend`/`deploy-broker` — colapsa el split de dos apps Fly en un solo job porque ahora es un único host. Hace login en GHCR con el propio `secrets.GITHUB_TOKEN` del workflow — a diferencia de Fly, no hace falta dar de alta ni cuenta ni token nuevos solo para el registro de imágenes —, construye la imagen del `Dockerfile` raíz y la publica con dos tags (`github.sha` y `latest`; el owner del repo, `HenrySanguna`, se hardcodea en minúsculas porque `ghcr.io` exige rutas de imagen en minúsculas y `github.repository_owner` no lo está). Luego, con SSH plano sin ninguna GitHub Action de terceros nueva: `rsync` copia `docker-compose.prod.yml` y `docker/mosquitto/mosquitto.prod.conf` a `/opt/fleetpulse/` en la VM preservando la estructura de directorios (`--relative`), y un comando SSH final ejecuta `docker compose -f docker-compose.prod.yml pull && ... up -d` con `IMAGE_TAG=<sha>`. Gate opt-in con el mismo patrón que las tandas anteriores pero con nombres nuevos: `vars.ORACLE_DEPLOY_ENABLED` (sustituye a `vars.FLY_DEPLOY_ENABLED`) y tres secretos nuevos — `ORACLE_SSH_HOST`, `ORACLE_SSH_USER`, `ORACLE_SSH_KEY` — en vez de `FLY_API_TOKEN`. Riesgo nuevo, no presente con Fly, documentado sin disimularlo: Fly terminaba TLS automáticamente en su proxy de borde (`handlers = ["tls", "http"]` en el `fly.toml` retirado); una VM de Oracle desnuda no lo hace. `docker-compose.prod.yml` publica `api` en el puerto 8080 en plano (HTTP, sin TLS) — un proxy inverso (Caddy o nginx) con TLS es trabajo real pendiente, deliberadamente fuera de esta tanda para no ampliar el alcance ni el presupuesto de líneas más allá de lo pedido; queda anotado para que no sorprenda a futuro. Pasos manuales pendientes, no realizables por esta sesión (sustituyen por completo a la lista de pasos de Fly de tandas anteriores): crear cuenta de Oracle Cloud; crear la VM Always Free Ampere A1 (riesgo de fricción conocido: la disponibilidad de capacidad Ampere A1 gratuita varía mucho por región y a veces exige reintentos durante días — señalado para que no sorprenda); configurar firewall/ingreso de la VM para el puerto de `api` (tanto el Security List/NSG de Oracle como `iptables`/`firewalld` dentro de la VM — Oracle filtra en ambas capas por defecto); generar y desplegar el par de llaves SSH; crear `/opt/fleetpulse/.env` en la VM con `SPRING_DATASOURCE_URL`/`SPRING_DATASOURCE_USERNAME`/`SPRING_DATASOURCE_PASSWORD` reales de Neon; añadir los secretos de repo `ORACLE_SSH_HOST`/`ORACLE_SSH_USER`/`ORACLE_SSH_KEY`; crear el proyecto de Cloudflare Pages `fleetpulse-console` y sus secretos (sin cambios respecto a antes); activar `vars.ORACLE_DEPLOY_ENABLED`/`vars.CLOUDFLARE_DEPLOY_ENABLED`. Verificado en esta tanda sin fingir ejecución real: `.github/workflows/ci.yml` completo parseado con `js-yaml`, los 5 jobs resultantes en el orden esperado (`affected`, `docker-and-compose-smoke`, `deploy-oracle`, `verify-deploy`, `deploy-console`); `docker-compose.prod.yml` no se pudo validar con `docker compose config` en esta sesión (Docker Desktop no está corriendo, mismo motivo ya documentado en la tanda anterior), revisado a mano contra la sintaxis ya válida de `docker-compose.yml` (dev) y contra los mismos patrones `environment`/`healthcheck`/`depends_on`/`networks` que ese archivo ya usa). Actualización posterior: el usuario completó los pasos manuales pendientes (cuenta Oracle Cloud, región Frankfurt, VM Always Free Ampere A1 `instance-20260907-1400` con IP pública efímera asignada, Security List + `iptables` del SO abiertos en 8080, Docker instalado, `/opt/fleetpulse/.env` con credenciales reales de Neon, secretos de repo `ORACLE_SSH_HOST`/`ORACLE_SSH_USER`/`ORACLE_SSH_KEY` y variables `ORACLE_DEPLOY_ENABLED=true`/`PROD_API_URL` configurados). El primer push con el gate activado disparó `deploy-oracle` real y encontró tres bugs reales, corregidos en el momento: (1) `nx-set-shas` fallaba en TODO push a `main` desde la tanda anterior (PR #9) con "Resource not accessible by integration" — necesita permiso `actions: read` para consultar el último run exitoso vía la API de Actions al diferenciar en push (a diferencia de `pull_request`, que diferencia contra la base del PR sin llamar a la API); pasó desapercibido porque `gh pr checks` solo expone el run de `pull_request`, nunca el de `push` a `main` — corregido añadiendo `actions: read` a los permisos del workflow (PR #12). (2) Con eso arreglado, `deploy-oracle` corrió pero `api`/`processor` quedaron en `Restarting` con `exec format error`: la VM Ampere A1 es **arm64**, el runner de GitHub Actions es amd64, y `docker build` sin `--platform` construye para la arquitectura nativa del runner — corregido con `docker/setup-qemu-action` + `buildx --platform linux/arm64` (PR #13; el build cruzado tarda ~14 min por la emulación QEMU, contra unos pocos segundos nativos). (3) Ya con la imagen correcta, `api`/`processor` seguían cayendo: `SPRING_DATASOURCE_URL` en `/opt/fleetpulse/.env` tenía las credenciales de Neon embebidas en la URL (`jdbc:postgresql://usuario:password@host/...`) — el driver JDBC de PostgreSQL no acepta userinfo embebido en la URL, hay que pasar usuario/password por separado en `SPRING_DATASOURCE_USERNAME`/`SPRING_DATASOURCE_PASSWORD`; corregido a mano en el `.env` de la VM (no es un bug de código, es configuración fuera del repo). Verificado en vivo, sin fingir nada: `curl http://92.5.91.123:8080/actuator/health` responde `status: "UP"` con `db`, `mqttBroker`, `commit` y `processorHeartbeat` (con `lastHeartbeat` fresco) todos `UP`; `curl .../actuator/info` confirma `git.commit.id.full` = SHA real desplegado. Nota de seguridad: la password de Neon quedó expuesta en texto plano durante el diagnóstico (pegada en la conversación) — se le indicó al usuario rotarla en la consola de Neon, pendiente de confirmación
- [x] 6.6 Verificación post-despliegue: SHA correcto y latido de `processor` reciente (`verify-deploy` en `ci.yml` hace `GET /actuator/info` comparando `.git.commit.id.full` contra `${{ github.sha }}`, y `GET /actuator/health` exigiendo `.components.processorHeartbeat.status == "UP"`, apuntando a `vars.PROD_API_URL` — mismas rutas JSON que `ActuatorInfoEndpointTest.java`/`ActuatorHealthEndpointTest.java` (tareas 5.2/5.3). Verificado manualmente en vivo mientras se diagnosticaban los tres bugs de 6.5 (`curl` directo contra `http://92.5.91.123:8080`, SHA y latido confirmados UP); pendiente una corrida de `verify-deploy` en CI limpia post-fix para la confirmación automática — no debería fallar, ya que los tres bugs que la bloqueaban están corregidos y el estado en vivo ya es correcto)

## Definición de terminado
- [x] `docker compose up` levanta PostGIS y Mosquitto; ambos procesos arrancan y se conectan al broker (verificado end-to-end real, ver tarea 6.4)
- [x] Un commit que solo toca `apps/console` NO dispara tareas de Gradle (no se ejecutó un commit dedicado console-only en este batch — el commit real de este PR tocó infraestructura de raíz, no `apps/console`, y el run real en CI (ver tarea 6.1) confirma que `nx affected` seleccionó únicamente `fleetpulse-backend`, sin mencionar `console`. Combinado con los edges reales del grafo de dependencias ya probados en la tarea 1.4, y con esta confirmación de que el propio mecanismo de `nx affected`/`nx-set-shas` funciona correctamente en el runner Linux de CI — la única duda pendiente, dado que no es reproducible en esta máquina Windows — el resultado para un commit console-only está determinado por el mismo algoritmo de traversal del grafo, ya verificado en sus dos mitades)
- [x] Un commit que toca `backend/domain` SÍ reconstruye `api` y `processor` (mismo razonamiento que el punto anterior: los edges `api -> domain` y `processor -> domain` están probados de forma directa en la tarea 1.4, y este PR confirma que el mecanismo de `nx affected` que recorre ese grafo funciona correctamente en CI real)
- [x] Cambiar un DTO del backend sin regenerar el cliente hace fallar el pipeline (`npm run generate:api-client:check` ahora cableado sin condiciones en el job `affected` de `ci.yml`; RED probado en la tarea 4.3, GREEN reverificado en esta tanda tras corregir el falso positivo de fin de línea descrito en 6.1)
